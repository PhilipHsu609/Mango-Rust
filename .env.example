# Environment Variables Template for Mango-Rust
# Copy this file to .env and adjust the values for your environment

# Database URL for SQLx compile-time verification
# This must point to an actual database file with the correct schema
# The database should have migrations applied before running cargo build
#
# Format options:
#   - Absolute path: sqlite:///absolute/path/to/mango.db
#   - Relative path: sqlite://./mango.db
#   - With options: sqlite:///path/to/mango.db?mode=rwc
#
DATABASE_URL=sqlite://./mango.db
